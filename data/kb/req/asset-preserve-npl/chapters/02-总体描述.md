<!-- source: P02天阳信贷管理系统-资产保全需求分册【不良资产清收处置】.docx | 标题路径: 不良资产清收处置管理 > 总体描述 -->

> 标题路径：不良资产清收处置管理 > 总体描述（功能说明 / 业务流程 / 状态说明）
>
> 要点摘要：资产保全业务人员获得审批、查询、新增、关联、修改等能力，支持不良资产全流程线上化；按用户角色和机构层级自动做数据权限控制。业务流程原文为「无」。状态说明给出三组通用状态机：流程状态、状态、审批状态。
>
> 关键规则/校验：通用状态流转「录入完成 → 提交申请 → 审批中；审批中 → 审批通过 → 已生效 / 审批否决 → 退回修改（草稿）」。审批中（审查审批中）允许「收回」；草稿允许「修改 / 取消申请」；审批通过允许「下载批复」——该口径贯穿各业务列表页。
>
> ZJJK：无。
>
> 待湿测：各业务实际状态字典 crgPcsSt 码值待湿测核对。

## 总体描述

### 功能说明

总体描述为资产保全业务人员提供审批、查询、新增、关联、修改等功能，支持不良资产全流程的线上化管理。有权限用户在任务页审批案件执行情况，查看基本信息和影像信息，签署审批意见后返回；提供新增及时重检认定的任务页，按基本信息、影像信息、签署意见三个步骤引导用户完成分类认定申请的录入、保存与流程提交。；诉讼登记执行情况提交流程页面，用于客户经理或有权限用户提交案件执行情况的审批流程。系统根据用户角色和机构层级自动执行数据权限控制，确保业务数据的访问安全与合规性。

### 业务流程

无

### 状态说明

流程状态 — 业务操作与审批流程驱动的状态标识

状态清单：

“审查审批中”的在途诉讼申请：业务状态的中间节点，具体操作取决于当前环节

审查审批中：已提交进入审批环节，允许收回

审批中：已提交进入审批环节，允许收回

下拉选择：业务状态的中间节点，具体操作取决于当前环节

审查审批中的在途诉讼申请：业务状态的中间节点，具体操作取决于当前环节

审查审批中的在途诉讼申请（包括诉讼申请、变更诉讼请求、撤诉申请）：业务状态的中间节点，具体操作取决于当前环节

状态变更说明：

审查审批中 → 审批通过→已生效

审查审批中 → 审批否决→退回修改

审批中 → 审批通过→已生效

审批中 → 审批否决→草稿

状态 — 业务操作与审批流程驱动的状态标识

状态清单：

查看状态时不展示：业务状态的中间节点，具体操作取决于当前环节

任务池模式且任务池状态为待处理：业务状态的中间节点，具体操作取决于当前环节

状态变更说明：

录入完成 → 提交申请 → 审批中

审批中 → 审批通过 → 已生效

审批中 → 审批否决 → 退回修改

审批状态 — 业务操作与审批流程驱动的状态标识

状态清单：

审批通过、处置类型为核销：业务状态的中间节点，具体操作取决于当前环节

状态变更说明：

录入完成 → 提交申请 → 审批中

审批中 → 审批通过 → 已生效

审批中 → 审批否决 → 退回修改

