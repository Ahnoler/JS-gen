# Draft-Traj Coach（旁路 SOP）

OpenCode / Cursor 操作手册：需求模块 → 原子草稿交易。  
**第一版只含 `skill/`**（质量标准已内嵌）；`src/` OpenCode plugin 后置。

| | |
|--|--|
| 入口 | `skill/SKILL.md` |
| 质量 | `skill/references/taskdraft-quality.md`（+ gold/thin 样例） |
| API 对照 | `skill/references/api-map.md` |

控制面默认 **4097**。不占执行机槽；终点是 draft，不是录制。
