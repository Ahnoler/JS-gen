# T4 阈值提案（先测后定，不继承 v1 floor —— 决策 A6）

> 2026-09-10 · 对跑命令：`node scripts/kb/recall-eval.mjs --fixture <v1|v2> --json`（DEFAULT_FIXTURE 保持 v1 不动，历史对跑零成本）
> 快照：`T4-v1.json` / `T4-v2.json`（v2 快照即 v2 冻结基线）

## ① v1/v2 对跑表（切换前必出）

| 指标 | v1（n=100+30） | v2（n=190+55） | 漂移解读 |
|---|---|---|---|
| Acc@1 | 0.740 | **0.600** | E 层 50 条全口语改写（无卡面词）首战 0.100，拉低聚合；属新判别面的真实起点 |
| Recall@5 | 0.847 | **0.725** | 同上 |
| MRR@5 | 0.784 | **0.653** | 同上 |
| nDCG@5 | 0.798 | **0.667** | 同上 |
| 拒答率 | 0.633 | **0.382** | 近域负样本 25 条（同业务域无卡）是设计出的 FP 压力面：34 FP 中 24 个来自近域新增——**这层就是用来暴露拒答分辨率缺口的** |
| 噪声 Acc@1 | 0.740 | **0.600** | 随 Acc@1 同步 |
| 分层 A | 1.000 | **1.000** | A 层零容错保持（55/55） |
| 分层 B | 0.233 | **0.400** | 新 B 条目（语料侧改写）比 v1 B 更可命中 |
| 分层 C | 0.867 | **0.900** | — |
| 分层 D | 0.933 | **0.900** | 新 D 码 5 条全中（18/20） |
| 分层 E | — | **0.100** | 新层；判别口语改写能力的基线（5/50） |
| 延迟 warm p95 | 0.154ms | 0.190ms | 预算 50ms 内 |
| 延迟 cold p95 | 4.115ms | 4.498ms | 预算 200ms 内 |

**结论**：v1/v2 分布差异显著且**方向可解释**（E 新层 + 近域负压面），继承 v1 floor 会产出无意义的红/绿——A6 的「重测」必要性被实测证实。

## ② 阈值提案表

floor 推导 = **v2 冻结基线（T4-v2.json 实测）− 不变的 D11 margins**（与 v1 §T4 同一方法论；margins 不动 = 门禁灵敏度不变）。A 层下限 0.95 为决策 A5 **已批准**项（唯一非推导值）。

| 指标 | v1 floor（现行） | v2 实测基线 | margin（不变） | **v2 提案 floor** | 理由 |
|---|---|---|---|---|---|
| Acc@1 | 0.600 | 0.600 | 0.05 | **0.550** | 0.600−0.05；E 层并入后的真实回归底线 |
| Recall@5 | 0.707 | 0.725 | 0.05 | **0.675** | 0.725−0.05 |
| MRR@5 | 0.614 | 0.653 | 0.08 | **0.573** | 0.653−0.08 |
| nDCG@5 | 0.628 | 0.667 | 0.08 | **0.587** | 0.667−0.08（margin 实值取自 runner DEFAULT_MARGINS，lockstep pin 实测纠偏） |
| 拒答率 | 0.583 | 0.382 | 0.05 | **0.332** | 近域负压面拉低基线；**floor 随拒答能力提升应只升不涨回**（T3/方向5 过泛化后 Lead 应上调） |
| 噪声 Acc@1 | 0.550 | 0.600 | 0.10 | **0.500** | 0.600−0.10（noise margin=0.10 实值） |
| **A 层 Acc@1** | （v1 无） | 1.000 | — | **0.95（A5 已批）** | 零容错探测器：55 条允许 miss ≤2 |
| warm p95 | ≤50ms | 0.190ms | — | ≤50ms（不变） | 绝对预算，非比例 |
| cold p95 | ≤200ms | 4.498ms | — | ≤200ms（不变） | 同上 |

## ③ 切换形态（本提案随门禁切换独立 commit 落地）

1. `characterize-kb-recall-eval.mjs` **显式**指向 v2 fixture（不再经 DEFAULT_FIXTURE）；`recall-eval.mjs` 的 `DEFAULT_FIXTURE` **保持 v1 一字不动**。
2. 结构断言升级为 spec §7 验收表：≥235 条 / 六层配额（A≥55 B≥45 C≥20 D≥20 E≥50 N≥55）/ 正≥180 / 覆盖≥70 卡 / 单卡≤4 / E 三子模式各≥15 / `relabels` 默认空 / 无陈旧 gold / negatives 带 whyNegative。
3. lockstep pin 更新为 v2 基线快照 `{0.600, 0.725, 0.653, 0.667, 0.382, 0.600}`（钉死 floor+margin 重构 == 冻结快照，两文件不可静默漂移）。
4. 新增 **A 层下限断言 ≥0.95**（D7/A5）。
5. 延迟预算与失败明细打印逻辑不变。

## ④ 批准状态与证伪自证

- **批准状态**：提案值随切换 commit 先行落地（v1 先例：G2 后置批准），**待 Lead 追认**；若 Lead 调整，只改 `APPROVED` 一处 + lockstep 字面量同 commit。
- **证伪自证**（T4-selfproof.txt）：阈值整体 +0.2（含 A 层 0.95→1.15）→ 门禁必红 exit 1（六项 floor 与 A 层同时 FAIL）→ 还原 → 绿；`--baseline tmp/kb-eval-v2/T4-v2.json` → exit 0。
