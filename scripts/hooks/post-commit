#!/bin/sh
# JS-gen post-commit hook（已停用）：Python 控制面同步已被叫停（2026-08-26），
# 不再自动跑 check_jsgen_sync.py。保留本文件仅为避免 .git/hooks/ 下的旧副本
# 继续执行已废弃的检查；如需移除可删除 .git/hooks/post-commit 与 scripts/hooks/post-commit。
#
# 历史行为（2026-08-26 前）：检测 schema/接口/WS 变更时自动跑
# d:/dev/ui-auto-recording-agent-python/scripts/check_jsgen_sync.py 打印报告。
exit 0
