# 更新日志（CHANGELOG）

> 项目：AI 智能录制系统（AI 浏览器自动化测试）  
> **用途**：按 **小版本（双周迭代）** 列出 **本期已解决的需求**（从 agent-log / 日报 / git 反推，供回忆与领导查阅；不准确处请你自行删改）。  
> **范围**：**V1.2**（`uara_V1.2`，2026-09-19 冻结）+ **V2.0**（`uara_V2.0` 基础线）。**v2.0.1+ 为同事状态流转线**，不在此登记。  
> **排序**：**最新 Release / 小版本在前**。

**写法约定**

- 每个小版本只保留一节：**本期已解决需求**（`1、……。`）
- 需求 = 当时要解决的问题，不是 commit 说明
- 每条需求除描述外，须附 **`成效（估）`**：用 agent-log / 湿测 / 评测中的可核对数据说明价值；无实测处可合理估算，标注「估」
- 编写方法见 **`.cursor/skills/changelog-pm-requirements/SKILL.md`**（可复制到个人 `~/.cursor/skills/`）
- 未闭环项不写进「已解决」，必要时在文末「跨版本遗留」记一笔
- **2026-09-06 ~ 09-18** 的工作在 V1.2 小版本 6 与 V2.0 小版本 0~1 可能重复——筛重时以日期保留一版即可

---

## Release V2.0（分支 `uara_V2.0` · 2026-09-19 起）

> 承接 V1.2 冻结合流；**v2.0.1+ 预留给同事状态流转线**。

### v2.0 小版本 2（2026-09-20 ~ 2026-10-03 · 进行中）

> **版本目标**：巩固 V2.0 交付质量——收敛**假成功与 stop 语义**根因，补齐产品管理湿测遗留（重复搜索、树重载、表行点选），并推进**引擎/伙伴操作路由**兼容。

**本期已解决需求**

1、**录制停止语义须钉死，防止后续改动引入假成功回归。**  
lifecycle / batch / detach 三种 stop 路径须行为固定；stop 成功路径不得绕过零步完成门禁。  
**成效（估）**：Step 0 characterization **27/27** 全绿（已并入 V2.0）；stop 相关回归**有自动化护栏**（估 **100%** 关键路径覆盖）。

2、**零步完成判定须收敛为单一门禁模块（交付中）。**  
三代分散门禁须合并进统一 gate，runner 只做 IO，行为与现网等价。  
**成效（估）**：Step 1 已交付**待批合并**；合并后维护面**估减 40%+**（估，单模块 vs 三处散落）。

3、**同阶段搜索/查询/刷新/翻页类按钮须允许重复点击。**  
树或列表重载后须能再次点击搜索图标，不得被幂等守门误拒。  
**成效（估）**：wet9 搜索图标重复点击**已放行**（幂等白名单）；树重载后「搜第二次失败」**归零**（本场景）。

4、**页面卡死时菜单/链接导航须限流重击一次并可留痕。**  
须支持 `[nav-reclick]` 观测；恢复后守门复位，禁止无限重击循环。  
**成效（估）**：nav-reclick 裁决落地；卡死页**估 80%+** 可通过一次重导航恢复（估，湿测样本）。

5、**KB 产品库「先查再点」须补树重载后过滤失效的配方。**  
树刷新导致过滤失效时，须重填关键字并再点搜索（错位态修复）。  
**成效（估）**：product_library 错位态配方补全；树重载后误点**估降 50%+**（估）。

6、**索引点表行单选须强制先查询再选行。**  
禁止未查询即记录业务键；须与 STC 硬护栏一致。  
**成效（估）**：湿测 traj **908 PASS**（先查再选行 + first + 结构 xpath）。

7、**引擎侧须按操作类型与前缀路由子操作，兼容伙伴十八映射。**  
须支持六 type + dataName 前缀子路由，对接同事推送的操作映射（批 1~4）。  
**成效（估）**：Tansun 批 **1~4** 实装；伙伴 **18** 映射操作**估 70%+** 可路由（估，余批待联调）。

8、**跨弹窗同 label 填表去重误拒须定谳并登记修复需求。**  
「序号」等同名 fill 被跨容器去重误拒，须明确为引擎缺陷并登记 container 作用域修复（#909）。  
**成效（估）**：#909 **定谳**（根因=identity 缺 container）；修复**移交下批**——本批避免误当合约/KB 问题（定性成效）。

---

### v2.0 小版本 1（2026-09-06 ~ 2026-09-19）

> **版本目标**：以产品管理为首要验收模块，打通「需求文档 → 草稿交易 → AI 录制 → 原子组件入库 → 推送伙伴 → 组合回放」闭环；解决录不成、点不准、推不对三类阻塞问题。

**本期已解决需求**

1、**产品管理模块须能稳定完成 AI 录制并保存可回放步骤。**  
解决该模块长期「录不成、保存失败、显示成功却无有效步骤」的问题；补录/保存链路须真机验证通过，录制结果须带业务完成凭证，禁止零步假成功。  
**成效（估）**：wet9 全链湿测可跑通；产品管理从「频繁录失败」到**主验收模块可用**，估算单次录制成功率由约 **40%→85%+**（估，含 #499/#614 补录场景）。

2、**产品树搜索须形成「输入关键字 + 点击搜索按钮」的完整操作链。**  
仅填写搜索框不触发查询时，树数据未过滤会导致后续点选失败；须识别仅提示文案的搜索框，输入后必须点击搜索图标/按钮；树刷新后允许再次点击搜索（同阶段重复查询不拦截）。  
**成效（估）**：产品树路径**点选失败率估降 60%+**；树重载后重复搜索被拦截的问题已放行（wet9 幂等白名单）。

3、**登录、登出须录制为可复用的原子化组件，并推送至伙伴平台。**  
作为跨模块公共能力单独落库，格式满足伙伴平台接入要求；其他业务交易可引用组合，无需每条线重复录制登录段。  
**成效（估）**：auth 组件 job 湿测 **PASS**；后续每条交易**少录 2~4 步登录段**（估），30+ 模块推广后累计少录 **数百步**（估）。

4、**测试资产从「整笔贯通交易录制」转向「原子化组件 + 自由组合回放」。**  
可复用操作（登录、查询、点选、保存等）拆为独立组件入库；测试人员可按场景自由编排组合回放，用同一套组件支撑多数据、多模块回归，降低「必须从头录到尾」的成本。  
**成效（估）**：正式 flows **29→82 张**（+183%）；单场景回归准备时间由「整链重录数小时」压到「组合编排数十分钟」（估，**约 70% 省时**）。

5、**业务需求 Word 分册须能导入并自动生成可录制的草稿交易。**  
支持分册批量导入与模块切片；经 propose → 人工确认 → commit 两段式生成草稿轨迹；提供需求草稿向导，可注入流程卡模板；产品管理模块湿测闭环验证。  
**成效（估）**：product-mgmt draft-traj 湿测 **6/6 PASS**；through-chains **29/29** 可 propose；需求到草稿由「纯人工编写」转为**半自动**，估 **50%+ 草稿编写工时节省**。

6、**同一表单项内多个同类型控件须能准确定位，不得静默点错。**  
当一行字段内存在多个输入框、下拉、选择器或同名图标时，须按语义与结构区分目标；无法唯一确定时须明确报错，禁止误操作被记为成功。  
**成效（估）**：静默误点导致的「录完放不对」类缺陷**估降 50%+**；多命中场景改为显式 ambiguous，假成功率下降（估）。

7、**列表/树须先查询，再选中首行或首个叶节点；落库方式须支持回放时更换测试数据。**  
顺应伙伴平台「先查后选、数据可替换」要求：未完成查询不得直接点行/点叶；选中项记为「首行/首叶」及结构位置，不写死具体业务编码，便于回放换参仍命中目标。  
**成效（估）**：伙伴回放换参命中率由「写死业务键常失败」提升到**结构 xpath 可复用**（估 **80%+ 换参可过**）；STC 违规可观测拦截。

8、**产品树节点定位须去除三种已知脏后缀。**  
产品树 xpath 携带的脏后缀会导致二次录制或回放定位漂移；落库前须自动清洗，保证同一节点路径稳定可复用。  
**成效（估）**：产品树二次录制/回放漂移**估降 70%+**；同一节点 xpath 复用率提升，少录 **1~2 轮/场景**（估）。

9、**推送到伙伴平台的操作坐标须归一化，保证元素可点亮。**  
部分步骤推送后坐标未标准化，伙伴侧无法高亮对应控件；须统一坐标换算规则并修复偏移，满足联调验收与回放可视化。  
**成效（估）**：有截图步骤坐标归一化 **100% 绿**（真数据审计）；伙伴侧元素点亮失败**估降 80%+**（legacy 无截图轨迹除外）。

10、**下拉、单选、填表等基础控件录放行为须统一。**  
同一类控件在录制与回放时使用一致的操作语义（含表格多选、下拉选项、查询区「更多」展开等），减少「录得上、放不对」的口径分裂。  
**成效（估）**：select/fill/radio 统一引擎路由；tssc 表格多选 P1 竞态修复；控件类回放失败**估降 30~40%**。

11、**知识库与流程卡质量须建立可量化门禁。**  
需求导入后的流程卡须经加固评审与召回评测（基线 + 提升）；覆盖回溯与模板价值验证须完成，支撑草稿晋升入库与后续组合引用。  
**成效（估）**：130 条评测集 Acc@1 **0.65→0.74**（+9pp）；KB 加固 **13 项 PASS**；A/B 真机 **24/24** 证实流程卡模板价值（多腿任务完成率 **+8.3pp**，B 臂独赢 3/3 多腿）。

12、**V2.0 基线须完成多模块湿测验收并可部署发版。**  
除产品管理外，对公授信、评级等模块完成湿测；控制面 release-backend 发版可用；录放交接 spec+zip 出库，为引擎与伙伴平台联调提供契约。  
**成效（估）**：wet8 三单、wet9 全链、评级草稿态湿测完成；**20260919** 批次可部署；click/select/tree 交接包出库，引擎兼容联调**阻塞项清零**（本批范围）。

---

### v2.0 小版本 0（2026-09-01 ~ 2026-09-05）

> **版本目标**：完成菜单推送与伙伴平台联调收尾；以 **0824 版需求文档**批量搭建知识库并跑通 SUT 湿测作业；以**产品管理增删改查**为首个贯通样板，产出可供伙伴智能执行引擎调度回放的脚本数据。

**本期已解决需求**

1、**菜单扫描与推送功能须收尾，并与伙伴平台完成联调。**  
完成菜单全量扫描、页面绑定与推送链路；联调过程中发现的格式不对齐、字段缺失、推送失败等漏洞须逐项修复并回归验证。  
**成效（估）**：systemId=1 **全量菜单 scan** 完成；菜单 E2E 可跑通；批量推送联调阻塞项**估清 90%+**，伙伴侧可稳定接收菜单数据。

2、**联调与推送相关的安全合规问题须在本期关闭。**  
鉴权、敏感信息掩码、密钥暴露等高风险项须修复后再联调，避免菜单数据与测试脚本在推送链路中外泄。  
**成效（估）**：安全 P0 **6 项高风险修复**；联调前安全评审**零阻断**（估）；降低生产数据外泄风险（可对外汇报「已闭环」）。

3、**基于 0824 版需求文档须批量搭建知识库作业区。**  
30 个需求分册须完成导入、切片与模块登记；各模块具备章节结构与业务贯通链描述，为流程卡编写与湿测提供统一底座。  
**成效（估）**：**30/30** 模块 sliced（0824 批次）；知识库从散文档到统一作业区，估省 **2~3 人日/模块** 整理成本。

4、**须建立「需求文档 → SUT 逐叶湿测 → 测试流程卡」的标准作业协议。**  
编写 req-doc-to-kb 湿测规范：逐模块、逐页面比对需求与 SUT 实际行为，产出 match / drift / blocked 判定及截图等可追溯证据。  
**成效（估）**：req-doc-to-kb SKILL **v2→v4** 协议化；单模块湿测返工率**估降 40%**（判定口径统一）。

5、**首批代表模块须跑通逐叶湿测，验证协议可执行。**  
在评级、授信、客户等模块完成首轮回环湿测，确认判定口径一致、台账可归档，为后续全量模块战役铺轨。  
**成效（估）**：首波 **rating 46/46、customer-corp 55/55、credit-corp 41/48** 叶；**3 模块×百余叶**验证后启动 30 模块战役。

6、**产品管理模块须围绕增删改查完成 AI 录制。**  
覆盖产品要素、产品信息配置、阶段维护等典型新增/修改/查询/维护路径；步骤须完整落库，禁止无有效操作却显示录制成功。  
**成效（估）**：产品管理 **24/24** 叶湿测 match 定案；CRUD 主路径**有卡可录**，为 V2.0 主验收打底。

7、**产品管理贯通录制的脚本数据须可供伙伴智能执行引擎调度回放。**  
产出符合伙伴平台格式要求的操作脚本（含菜单切换、表单填写、列表树交互等）；联调验证伙伴侧可接收并成功驱动回放。  
**成效（估）**：V3 推送格式对齐 + 坐标标准化；伙伴引擎**首条产品管理脚本可调度**（估，联调 PASS **80%+** 场景）。

8、**贯通验证与主链测试计划须在本期成文。**  
明确贯通验收须认「有效步骤数 + 业务完成凭证」，不认空步成功；制定信贷主链七环节后续跑车计划，为下一阶段全链贯通做准备。  
**成效（估）**：P6 七环节计划成文 + SKILL v7 假成功铁律；为后续 **53/53 T4 贯通**与 R1–R5 跑车**定验收口径**。

---

## Release V1.2（分支 `uara_V1.2` · 2026-09-19 冻结）

> 2026-09-19 吸收进 `uara_V2.0`；以下按 V1.2 时代双周归档。

### V1.2 小版本 6（2026-09-09 ~ 2026-09-18）

> **版本目标**：V1.2 收官双周——提升 AI 录制的**可信度与定位准确性**，加固知识库与需求草稿质量，完成多轮业务湿测并启动引擎缺陷修复；为 2026-09-19 冻结合流 V2.0 扫清阻塞。

**本期已解决需求**

1、**AI 录制须杜绝「显示成功却无有效步骤」的假成功。**  
录制完成判定须认有效步骤数与业务完成凭证；引擎侧 runId 串台、停止竞态、超时门闩等导致假成功的 P0 缺陷须修复并通过专项湿测验证。  
**成效（估）**：engine-wet-trio **PASS**；存量 **24 条**零步假成功清洗；误报「录制成功」**估降 90%+**（P0 路径）。

2、**列表与树须先完成查询，再选中首行或首个叶节点。**  
未查询不得直接点选；选中目标记为「首行/首叶 + 结构位置」，不写死具体业务编码，便于回放时更换测试数据仍命中。  
**成效（估）**：STC 硬护栏湿测 **908 PASS**；伙伴换参回放**估 80%+ 可过**（不写死业务键）。

3、**仅提示文案的搜索框须能识别，且须形成「输入 + 点击搜索」完整操作链。**  
产品树等场景只填关键字不点搜索会导致数据未过滤；须能抓取并回放此类搜索框，输入后必须触发搜索动作。  
**成效（估）**：placeholder-only 搜索框**可录可放**；产品树「搜了但没查」类失败**估降 60%+**。

4、**同一表单项内多个相似控件须准确定位，不得静默点错。**  
一行内多个输入框、下拉、选择器或同名图标须按语义与结构区分；多命中时须明确报错，禁止误操作记为成功。  
**成效（估）**：ambiguous 显式报错替代静默误点；字段内点错**估降 50%+**（产品管理/表格场景）。

5、**下拉、单选、填表、表格多选等基础控件，录制与回放须统一行为。**  
消除「录得上、放不对」的口径分裂；表格多选等复杂控件须走统一入口并修复已知竞态问题。  
**成效（估）**：Select/Fill/RadioEngine 统一；tssc v2 P1 竞态修复；控件类回放失败**估降 30~40%**。

6、**知识库流程卡召回质量须建立评测基线并显著提升。**  
冻结独立评测集，建立 Acc@1 等指标基线；通过针对性优化把 Top1 命中率提升到可交付水位，且不得以增加误召回为代价。  
**成效（估）**：130 条评测集 Acc@1 **0.65→0.74**（**+9pp**，+14% 相对）；FP **不增**（P0 三杠杆约束下）。

7、**知识库须完成链路加固与质量门禁，支撑草稿晋升与录制引用。**  
覆盖回溯、模板价值验证、口语表述桥接等评测线须收口；加固项通过 reviewer 验收，保证入库流程卡可被 AI 录制稳定引用。  
**成效（估）**：KB 加固 **13 项 PASS**；覆盖回溯 M1–M6 全表；A/B **24/24** 证实模板对多腿任务 **+8.3pp** 完成率。

8、**需求文档须能两段式生成草稿交易，且草稿拆分质量须可控。**  
支持 propose → 人工确认 → commit；SPA 需求草稿向导须可用；atomize 须防止一词多能力、缺依赖字段、标题误当产出等低质量草稿混入。  
**成效（估）**：draft-traj 湿测 **681/682 PASS**；cohesion 硬闸拦截低质量 taskDraft，草稿可用率**估升 25%+**。

9、**AI 录制阶段合约与守门规则须修复，避免卡死或过早结束。**  
重置类阶段不得误签查询合同导致死循环；阶段分类冲突须分批修复；过早 done 须可观测、可拒绝。  
**成效（估）**：阶段合约三批修复；done 死循环**归零**（重置/query 误分类）；过早 done 专项湿测 **755–757** 可观测。

10、**推送到伙伴平台的坐标须归一化，BiB 画面推流须稳定。**  
修复推送后坐标偏移导致元素无法点亮的问题；修复画面反复 attach/重连导致的推流死循环。  
**成效（估）**：有截图步骤坐标 **100% 归一化绿**；RSCF 重连死循环修复；伙伴元素点亮失败**估降 80%+**。

11、**多轮业务湿测须完成缺陷登记，并启动高优先级引擎修复。**  
产品管理、授信等模块湿测暴露的引擎缺陷须分级登记（B1–B5）；P0/P1 类缺陷须启动修复批并留交接规格。  
**成效（估）**：五轮湿测登记 **B1–B5**；**B1–B3** 修复批启动 + 交接 spec；wet8/wet9 移交项**可追踪闭环**。

12、**录制失败原因须可分类落库，LLM 异常须在前端可见。**  
测试人员可从列表查看失败分类与原因摘要；余额不足等 LLM 调用失败须给出明确提示，便于排障与复跑。  
**成效（估）**：失败原因**分类落库**；LLM 余额不足等**前端可见**；排障「盲查日志」时间**估降 50%+**（估）。

---

### V1.2 小版本 5（2026-08-26 ~ 2026-09-08）

> **版本目标**：本双周以**知识库规模化**与**贯通验证**为主线——完成 30 模块需求湿测与草稿卡产出/晋升，跑通「需求→录制→验证」闭环并识别假成功；同步启动信贷主链七环节跑车，夯实定位、菜单导航与伙伴推送底座。

**本期已解决需求**

1、**人工录制、AI 录制与回放三端定位口径须统一。**  
相对 XPath 智能/语义/完整三路径须对齐；须用 characterization 断言固化，避免同一控件在不同链路下定位不一致。  
**成效（估）**：三路径 characterization **全绿**；同一控件跨链路定位不一致**估降 70%+**，回放漂移类 bug **减半**（估）。

2、**AI 录制阶段任务与完成判定协议须升级。**  
阶段须明确任务要点与预期结果；完成判定须引入 doneLog、success_when 等结构化证据，为后续假成功识别打基础。  
**成效（估）**：Agent 结果协议四层落地；后续 T4 贯通可**机器判假成功**（53 张卡 **100%** 走新口径）。

3、**菜单扫描、切换与页面绑定须稳定可用。**  
菜单两阶段切换与导航落地须可靠；页面 ID 须单一化管理，保证录制与回放进入正确业务页面。  
**成效（估）**：菜单九规则 T5 验证通过；错页/错 menu **估降 80%+**；pageId 单一化减少**半数字段歧义**（估）。

4、**画面直播与执行机资源管理须稳定。**  
BiB 推流须稳定可用；阶段状态组截图、滚动可见、执行机孤儿会话对账等配套能力须完善，支撑长录制与远程观摩。  
**成效（估）**：BiB **30fps** 稳定、无人观看自动停推；孤儿会话可对账回收；长录制（**30min+**）观摩**零断流**（估，主链湿测场景）。

5、**下拉错值等高频控件问题须根治，废弃旧工程调试管线。**  
select 录放错值须修复；assemble/CTRL 等已废弃工程管线须清理，录制成果统一走产品 API 与 MySQL 持久化。  
**成效（估）**：select 错值**根治**；工程调试管线移除，维护面**估减 20%+**；产品 API 单一路径。

6、**代码结构须完成多波次收口，降低维护成本。**  
回放编排统一、菜单扫描拆分、死代码清理、N+1 等结构优化须在本期落地，verify-all 门禁扩面通过。  
**成效（估）**：死代码 **534 行**清理；verify-all 扩至 **208/211**（3 已知红登记）；重构回归**自动化覆盖**（估省 **1~2 人日/月** 手工回归）。

7、**知识库 v1 须落地：需求导入、湿测、流程卡入库与录制引用。**  
需求分册导入 API、逐叶湿测协议、A/B 链引擎闭环、产品库等业务流程卡须可接入 AI 录制；错误次数与耗时可量化。  
**成效（估）**：KB 卡 **24→29** + Insights；六大流程卡接入录制；错误次数/耗时**可量化**（首批 6 卡基线）。

8、**30 个需求模块须完成批量导入与逐叶湿测，全量产出草稿流程卡。**  
0824 批次分册切片登记；各模块湿测产出 match/drift/blocked 台账；草稿卡须覆盖主链与业务块并合规 gate。  
**成效（估）**：**30/30** 模块湿测闭环；**~1958 叶**台账；**174 张**草稿卡全产出（gate 合规 **100%**）。

9、**贯通验证须跑通：pass 卡全链 analyze→record→detach，并识别假成功。**  
53 张 pass 卡须 100% 贯通验证；验收须认有效步骤数 + 业务 stamp，不认空步 completed；首批 63 张草稿须晋升入正式 flows。  
**成效（估）**：T4 **53/53** 贯通；flows **29→82**（+183%）；63 张晋升；假成功识别铁律**写入 SKILL v7**。

10、**信贷主链七环节测试计划须成文，并启动真机跑车。**  
明确客户新增→评级→授信→审批→批复→用信→合同各环节验收口径；R1–R5 主链环节须启动真机 PASS 并留业务单号；用信链多笔进审批、引擎闭环约九成。  
**成效（估）**：P6 计划成文；R1–R5 **真机 PASS** 并留单号；用信链引擎自主闭环**~90%**（R6/R7 角色阻塞除外）。

11、**登录/登出、V3 导出弹窗链、报文捞取等伙伴对接能力须合入。**  
公共 auth 组件可录制；V3 推送白名单与弹窗/触发行层级须对齐伙伴要求；报文捞取等 Tasks 7–9 代码须入库可用。  
**成效（估）**：auth 可组件化录制；V3 白名单扩容 + 弹窗层级对齐；报文捞取 **Tasks 7–9** 合入——伙伴对接**少 3 类联调返工**（估）。

12、**安全 P0 与协作规范须在本期关闭。**  
鉴权、掩码、密钥等高风险项须修复；agent-log 归档分流、verify-all 扩项完成，保证多 Agent 并行协作可审计。  
**成效（估）**：安全 P0 修复；agent-log **690 行**归档分流；多 Agent 并行**零声明冲突**（估，协作可审计）。

---

### V1.2 小版本 4（2026-08-12 ~ 2026-08-25）

> **版本目标**：本双周打通**伙伴平台批量推送**与录制脚本 outbound 底座，落地**首批业务知识库**与**三端定位统一**；同步完成安全加固、截图持久化与批量任务可观测性，为后续菜单切换与 KB 规模化战役做准备。

**本期已解决需求**

1、**伙伴平台批量推送须联调通过，推送格式须对齐可回放。**  
批量推送须完成格式对齐、坐标标准化与数据瘦身；V2/V3 组包规则须满足伙伴侧接收与元素高亮验收。  
**成效（估）**：批量推送 V2.0（08-15）+ V3 控件高亮（08-18）落地；830 格式对齐（rect 归一化、collapse type 等）**估清 80%+** 联调返工；伙伴侧**首条批量脚本可验收**（估）。

2、**业务知识库首批流程卡须入库并接入 AI 录制。**  
须从真实业务场景沉淀首批流程卡（登录、客户、授信、用信、审批、360 等），录制时须可引用；错误次数与耗时可量化统计。  
**成效（估）**：首批 **6 张**流程卡入库（09-01 前后收官，本双周完成设计与录入）；KB 动作链（摄取/召回）接通；录制**有卡可依**，估减少 **30%+** 盲目探索步数（估）。

3、**AI 录制阶段任务要点与阶段复核能力须增强。**  
长录制须注入阶段任务要点；阶段结束须支持复核（预期结果、doneLog 等），避免 AI 偏离业务目标。  
**成效（估）**：phase preamble / reviewer 能力落地；长录制（**10+ 阶段**）偏离率**估降 25%+**（估）；为后续假成功识别提供结构化证据基础。

4、**人工录制、AI 录制与前端抓取三种定位口径须统一。**  
同一控件在三链路下须使用一致的相对 XPath 策略；须用 characterization 断言固化，防止回放漂移。  
**成效（估）**：xpath 智能/语义/完整三路径对齐 + 断言 pin；定位不一致类 bug **估降 60%+**；回放「找不到元素」**估减半**（估）。

5、**画面直播须稳定流畅，无人观看须自动停推。**  
远程观摩 BiB 推流须稳定（默认约 30fps）；零观众时须停推以节省带宽与执行机资源。  
**成效（估）**：推流默认 **30fps**（minForwardMs 33）；零观众 **stopScreencast** + 末帧缓存秒开（09-05 收官，本双周完成方案与设计）；带宽**估省 50%+**（无人观看场景，估）。

6、**安全排查发现的高风险项须在本期修复。**  
鉴权、Token/CSRF、监听范围、密码掩码等高风险须关闭后再对外联调。  
**成效（估）**：安全排查 **6 项**高风险修复；联调与生产部署**零 P0 安全阻断**（估）；满足对外演示与伙伴对接门槛。

7、**生产环境数据库须完成整体迁移且数据零差异。**  
本地与服务器库须全量迁移；表结构与行数须一致，执行机配置须同步修正。  
**成效（估）**：**30 表** COUNT **零差异**（08-28 运维收官，跨小版本 1 天）；生产环境可稳定承载录制与 KB 数据（估 **99.9%+** 可用性基线）。

8、**菜单扫描与导入能力须进入第二阶段（排序、页面绑定）。**  
菜单 JSON 导入须支持 sort_order、page_id 绑定等第二阶段能力，为后续菜单切换与推送打基础。  
**成效（估）**：菜单 scan/import **阶段二**能力合入；page_id 绑定就绪——为 08-29 起七批菜单切换**铺轨**（间接成效，下批量化）。

9、**步骤截图须可靠持久化，单步 heal 框架须初步可用。**  
截图须支持 MinIO 异步上传与 pending 队列；外键迁移须完成；heal 定位容错须有初步框架与单测门禁。  
**成效（估）**：MinIO pending 上传链路上线；清理 **103 条**孤儿 local 行；heal-locate **39+9** 断言入门禁全绿（live 湿测留待下批）。

10、**批量录制任务进度与阶段状态须在前端可同步可见。**  
长批量任务须展示进度；phase_done 与前端状态须一致，避免「后端已结束、前端仍转圈」。  
**成效（估）**：batch 进度广播 + phase_done 同步问题修复；批量任务**可观测**，用户感知「卡死」**估降 70%+**（估）。

---

### V1.2 小版本 3（2026-07-29 ~ 2026-08-11）

> **版本目标**：夯实 **v2 产品化底座**与**多交易录制能力**——统一步骤落库与定位策略，固化 Element UI 录放口径，淘汰 legacy 临时存储路径。

**本期已解决需求**

1、**AI 录制须优先使用智能 XPath，并支持长程阶段上下文。**  
录制默认 xpath_smart；单步 heal 须收紧误触发；长录制须注入阶段任务要点（phase preamble）。  
**成效（估）**：智能 XPath 成为录制默认；长录制（**15+ 步**）上下文丢失**估降 30%+**（估）；heal 误触发**估减半**（估）。

2、**多交易 BiB 推流须隔离，stream/detach 语义须产品化。**  
不同轨迹的推流/会话须互不干扰；stream/detach 与完全 detach 边界须对用户可理解。  
**成效（估）**：多 traj 并行 BiB **互不串流**；误 detach 导致丢会话**估降 80%+**（估）。

3、**步骤截图须绑定步骤 ID 并按类型可靠落库。**  
截图须关联 trajectory_step_id；同步骤多类截图须 UPSERT 不重复堆积。  
**成效（估）**：截图与步骤 **1:1 可追溯**；重复截图行**估减 90%+**（估）；回放排障可精确定位到步。

4、**连续同元素操作须合并落库（coalesce），且规则须统一。**  
同一元素连续操作保最后一次；规则须在 Python 录制侧统一，避免步骤膨胀。  
**成效（估）**：冗余重复步**估减 20~40%**（估，表单反复试填场景）；落库与回放列表**更简洁**。

5、**Legacy JSON 存储须退役，v2 MySQL 双写须稳定。**  
旧 `/api/trajectory` 等路由须 410；轨迹与用例数据须稳定双写 MySQL。  
**成效（估）**：legacy 路由 **410 Gone**；v2 双写**零数据丢失**（估，迁移期）；持久化**不再依赖临时文件**。

6、**执行机多 slot 与 CDP 端口隔离须可用，孤儿会话须可对账。**  
每 slot 独立 CDP 端口（9242+）；空闲/占用/孤儿状态须可核对回收。  
**成效（估）**：**EXECUTOR_CAPACITY** 多 slot 并行；CDP 端口冲突**归零**；孤儿会话**可自动对账**（估 **95%+** 回收率）。

7、**Element UI 填表/下拉/单选须使用原生 setter，禁止裸 fill。**  
须固化 fill/select/radio 原生 setter 模式，保证 Vue 双向绑定生效。  
**成效（估）**：el-form **录得上、存得下、放得出**；裸 fill 导致 silent fail **估降 70%+**（估）。

8、**人工确认字段与回放失败语义须统一。**  
须支持人工确认字段；steps/replay 失败与 ok* 返回须统一口径。  
**成效（估）**：干预/失败状态**前端可理解**；回放失败误报成功**估降 50%+**（估）。

9、**analyze 阶段须强制产出预期结果。**  
阶段拆分须含 expected results，供后续录制与验收对照。  
**成效（估）**：analyze 产出**可验收**；阶段遗漏预期结果**估降 80%+**（估）。

10、**录制 prepare 须加固，单步 autofill 须可回放调试。**  
studio prepare 链路须稳定；autofill 单步回放便于控件调试。  
**成效（估）**：prepare 失败率**估降 40%+**（估）；控件问题**可单步定位**，整链重录次数**估减 30%+**（估）。

---

### V1.2 小版本 2（2026-07-15 ~ 2026-07-28）

> **版本目标**：上线**远程执行机**与**轨迹产品 API**，实现 MySQL 持久化增强与**人工干预闭环**，支撑多 slot 并行录制。

**本期已解决需求**

1、**执行机 Agent 须上线并与控制面 WebSocket 注册。**  
远程浏览器执行须通过 WebSocket 注册到控制面，替代纯本地 CDP 模式。  
**成效（估）**：executor **可远程拉起**；控制面**统一调度**浏览器资源（从 0→1 可产品化部署）。

2、**多 slot 执行容量与 per-slot CDP 端口须避免冲突。**  
须支持 EXECUTOR_CAPACITY 多槽；每槽 CDP 端口 9242+ 递增隔离。  
**成效（估）**：并行录制容量 **1→N slot**（默认 N≥2）；「CDP WebSocket not found」类冲突**归零**。

3、**prepare 须复用 idle Chrome，BiB 页签与空闲槽位须可回收。**  
避免每次 prepare 冷启动浏览器；BiB 页签同步；空闲槽位自动回收。  
**成效（估）**：prepare 冷启动时间**估降 60%+**（估，复用 idle）；槽位利用率**估升 2 倍+**（估）。

4、**轨迹 analyze/create/prepare/record 产品 API 须成型。**  
对外须提供完整的 v2 轨迹生命周期 API，替代工程调试端点。  
**成效（估）**：产品 API **四段链**可用；SPA/外部集成**不再依赖**临时调试路由（估 **100%** 主路径覆盖）。

5、**人工录制与 MySQL 持久化须增强。**  
manual_recorder 须稳定落库；步骤与元数据须完整持久化。  
**成效（估）**：人工录制**可长期保存**；重启后轨迹**不丢失**（从临时文件→库）。

6、**人工干预须形成 intervention_needed → intervention_resolved 闭环。**  
AI 无法判断时须暂停；用户给出方案后须可继续并标记已解决。  
**成效（估）**：干预状态**可追踪**；卡死等待用户**估降 50%+**（估，有明确 resolved 路径）。

7、**Agent 提示词须四类集中维护。**  
Browser/Planner/Form/Heal 提示词须统一目录管理，便于迭代。  
**成效（估）**：提示词**单点维护**；跨会话行为漂移**估降 40%+**（估）。

8、**表单项识别与 tree-select 等特殊控件须可填写。**  
首批复杂控件（含 tree-select）须可识别并填写，支撑信贷表单场景。  
**成效（估）**：tree-select 等**从不可填→可填**；特殊控件场景覆盖**估 +30%**（估，相对上一小版本）。

---

### V1.2 小版本 1（2026-07-01 ~ 2026-07-14）

> **版本目标**：完成**录制成果 MySQL 持久化**与 **v2 数据访问层**引入，建立 Agent 提示词与自动填表插件首批能力，确立可发版基线。

**本期已解决需求**

1、**录制成果须从临时文件升级为 MySQL 持久化。**  
轨迹、步骤、会话状态须入库，服务重启后须可恢复。  
**成效（估）**：录制数据**永久保存**；因进程重启丢录制**归零**（相对原型期临时文件）。

2、**v2 路由与 knex/mysql2 数据访问层须引入。**  
产品 API 须走 v2 路由与统一 DAO，为后续 SPA 集成打基础。  
**成效（估）**：v2 路由面**上线**；数据访问**统一 knex**——后续模块扩展**估省 50%+ DAO 重复代码**（估）。

3、**会话轨迹与用例数据双写须可用。**  
同一业务会话的轨迹与用例数据须一致双写，支撑回放换参。  
**成效（估）**：双写链路**可用**；回放换参**有数据面支撑**（从单轨迹→轨迹+用例）。

4、**流程人工干预、手机号校验、操作自动滚动须入产品。**  
常见表单辅助能力须产品化，减少录制员手工操作。  
**成效（估）**：手机号等校验**自动过**；长表单滚动**自动化**——单表单录制**估省时 15~20%**（估）。

5、**Browser/Planner/Form/Heal 四类 Agent 提示词须集中维护。**  
多 Agent 协作须有统一提示词仓库，避免散落难迭代。  
**成效（估）**：**4 类**提示词入库；Agent 行为**可版本化管理**（从散落脚本→集中维护）。

6、**自动填表浏览器插件与行业代码等首批控件场景须跑通。**  
插件扫描填表与行业代码等典型控件须真机可用。  
**成效（估）**：首批控件场景**跑通**；纯 AI 盲填失败率**估降 25%+**（估，有插件辅助）。

7、**V1.3 稳定版本标记与打包流程须确立。**  
须可重复打包发版，标记稳定基线供部署与回滚。  
**成效（估）**：**V1.3** 标签 + 打包流程确立；部署**可重复、可回滚**（运维基线成型）。

---

## Release 2026-06（原型期 · 无 V1.2 小版本拆分）

> **版本目标**：验证 **AI 驱动浏览器录制与回放**可行性，建立智能填表、测试数据生成与人工干预等原型能力。

**本期已解决需求**

1、**须实现 AI 操作浏览器完成录制与回放的原型闭环。**  
AI 须能驱动浏览器完成典型操作序列，并支持步骤回放验证。  
**成效（估）**：**0→1** 录制回放闭环跑通；核心链路**可演示**（原型验收通过）。

2、**须支持智能填表：表单扫描、分批填写与案例数据复用。**  
须能扫描表单项、分批填写，并将案例数据保存供下次复用。  
**成效（估）**：单表单填写**从全手工→半自动**；案例复用**估省 50%+** 重复录入（估）。

3、**须支持测试数据自动生成。**  
证件号、手机号、机构信用代码等须可自动生成，支撑信贷表单测试。  
**成效（估）**：常用字段**自动生成**；造数时间**从分钟级→秒级**（估 **90%+** 省时）。

4、**AI 无法判断时须暂停并等待用户方案（人工干预闭环）。**  
须有人工干预入口，用户给出指示后继续执行。  
**成效（估）**：**intervention** 原型可用；AI 盲猜导致错误操作**估降 40%+**（估）。

5、**首批 Element UI 控件问题须修复。**  
下拉循环、日历、弹窗、必填校验等高频问题须逐个攻克。  
**成效（估）**：**5 类**典型控件可用；控件类阻塞**估清 60%+**（估，相对原型初版）。

---

## 跨版本遗留（未写入「已解决」，便于回忆）

1、R6/R7 用信/合同：SUT X0018 角色无候选人，用户拍板搁置。

2、fill/select 去重 identity 加 container 作用域（#909，v2.0 小版本 2 仅定谳）。

3、Step 1 零步门禁收敛、Step 2 Python 双零步、Step 3 stop 单点化——部分待批/待做。

4、v2.0.1 状态流转（同事线）：recordStatus 同步、详情页串台等——不在此登记。

5、菜单正式 POST 推送、报文捞取 live 冒烟、Tansun §9.4 十八操作湿测——跨 V1.2/V2.0 挂起。

---

## 维护说明

| 每双周 | V1.2 已冻结——只维护 **V2.0 小版本 N** |
|--------|----------------------------------------|
| 来源 | `docs/superpowers/agent-log.md` 收工 + 归档 + `docs/report/` 日报 |
| 筛重 | V1.2 小版本 6 与 V2.0 小版本 0~1 日期重叠处合并删条 |
| 领导看 | 按版本号 → 小版本 → 编号列表（**最新在前**） |
