/**
 * Re-export the browser-session route registration from the modular
 * `browser-session/` subdirectory. Kept as a thin shim for existing imports.
 */
export { default } from './browser-session/index.js';
