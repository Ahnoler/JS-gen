# CHANGELOG 编写示例（本仓库金样例）

## 版本目标示例

```markdown
> **版本目标**：以产品管理为首要验收模块，打通「需求文档 → 草稿交易 → AI 录制 →
> 原子组件入库 → 推送伙伴 → 组合回放」闭环；解决录不成、点不准、推不对三类阻塞问题。
```

```markdown
> **版本目标**：V1.2 收官双周——提升 AI 录制的**可信度与定位准确性**，加固知识库与
> 需求草稿质量，完成多轮业务湿测并启动引擎缺陷修复；为 2026-09-19 冻结合流 V2.0 扫清阻塞。
```

## 单条需求：完整三段

```markdown
6、**知识库流程卡召回质量须建立评测基线并显著提升。**
冻结独立评测集，建立 Acc@1 等指标基线；通过针对性优化把 Top1 命中率提升到可交付水位，
且不得以增加误召回为代价。
**成效（估）**：130 条评测集 Acc@1 **0.65→0.74**（**+9pp**，+14% 相对）；FP **不增**
（P0 三杠杆约束下）。
```

## 单条需求：实测 + 估算混合

```markdown
1、**产品管理模块须能稳定完成 AI 录制并保存可回放步骤。**
解决该模块长期「录不成、保存失败、显示成功却无有效步骤」的问题；补录/保存链路须真机
验证通过，录制结果须带业务完成凭证，禁止零步假成功。
**成效（估）**：wet9 全链湿测可跑通；产品管理从「频繁录失败」到**主验收模块可用**，
估算单次录制成功率由约 **40%→85%+**（估，含 #499/#614 补录场景）。
```

## 单条需求：间接成效（避免跨版本重复计数）

```markdown
8、**贯通验证与主链测试计划须在本期成文。**
明确贯通验收须认「有效步骤数 + 业务完成凭证」，不认空步成功；制定信贷主链七环节后续
跑车计划，为下一阶段全链贯通做准备。
**成效（估）**：P6 七环节计划成文 + SKILL v7 假成功铁律；为后续 **53/53 T4 贯通**与
R1–R5 跑车**定验收口径**。
```

## 反例 → 正例

| 反例（技术/commit 口径） | 正例（产品需求口径） |
|--------------------------|----------------------|
| record/start 假成功须硬门闩：0 步不得标 completed | AI 录制须杜绝「显示成功却无有效步骤」的假成功 |
| select/fill/radio 须 Phase A→B 统一引擎 | 下拉、单选、填表等基础控件录放行为须统一 |
| characterize-stop-semantics.mjs 27/27 pin | （不写进需求；成效可写）Step 0 pin **27/27** 防改坏 |
| fix: fill_engine UnboundLocalError | （不写；若用户可见则写）录制失败原因须可分类落库 |

## 跨版本遗留示例

```markdown
## 跨版本遗留（未写入「已解决」，便于回忆）

1、R6/R7 用信/合同：SUT 审批角色缺失，用户拍板搁置。
2、fill/select 去重 identity 加 container 作用域（#909，仅定谳，修复待做）。
3、v2.0.1 状态流转（同事线）：不在此登记。
```

## 小版本篇幅建议

| 双周工作量 | 需求条数 | 版本目标 |
|------------|----------|----------|
| 轻（补丁/联调） | 5~8 | 1 句 |
| 中（单模块验收） | 8~12 | 1~2 句 |
| 重（KB 战役/收官） | 10~14 | 2 句 + 主题词 |

条目过多时按业务域合并，成效行可写 2 个指标，避免一条成效写 5 个数字难以阅读。
