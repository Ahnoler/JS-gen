# L1c-wet 报告（2026-09-11）

## 结论

**PASS** — `L1C_LLM=true` 下低置信 feature card 走 LLM；高置信走 rule；同卡二次命中 L1d。

| 检查 | 结果 |
|------|------|
| 控制面加载 `L1C_LLM=true` | 是（重启后生效） |
| 执行机 LMY connected | 是 |
| `POST /api/v2/regions/classify` 低置信 `source=llm` | PASS（`other` → `section`，confidence 0.55） |
| 同 systemId 二次 `source=l1d` | PASS |
| 高置信 `shell-aside` `source=rule` | PASS |
| BiB `resolve-element` inventory（traj 678） | 通路 OK：60 matches，`region_role` 已打标；本页仅 `shell-header`/`main`（高置信，不触发 LLM） |

## 环境

- 控制面：`http://127.0.0.1:4097`（本会话重启以加载 env）
- `config/.env`：`L1C_LLM=true`，`L1C_LLM_MODEL=GLM-5`，BASE_URL/API_KEY 指向与主 LLM 同网关
- 执行机：LMY (`2f21bad1-…`) online
- 脚本：`tmp/l1c-wet/wet-classify.mjs`（gitignore；可本地复跑）

## API 证据摘要

低置信首调（节选）：

```json
{
  "role": "section",
  "label": "客户综合评价辅助区 (customer evaluation assist panel)",
  "confidence": 0.55,
  "source": "llm"
}
```

高置信：

```json
{ "role": "shell-aside", "label": "系统菜单", "confidence": 0.9, "source": "rule" }
```

## BiB 备注

traj **678** 已附着产品管理页；`POST .../resolve-element` `mode=inventory` → 200 / 60 matches，feature_card 经 `applyL1cRegionClassify` 后剥离（符合设计）。当前页分区均为高置信，故 BiB 未再打出 `source=llm`——LLM 路径以共用 classify 服务湿测为准（resolve/scan 同 `classifyRegions`）。

## 遗留

- 若需「页面上真有 other 区再看 BiB LLM 改写」：换到含折叠/自定义面板的页面再跑 inventory。
- `config/.env` 现为 `L1C_LLM=true`；不需要时改回 `false` 并重启控制面。
