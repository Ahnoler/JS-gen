# 产品管理线贯通状态报告（2026-09-07）

> 目标：**周三（2026-09-09）下班前跑通产品管理全链**——KB 五叶 → 交易录制 → 回放验证 → V3 推送。
> 范围限定：本报告只覆盖产品管理线；信贷主链（⑤）不在本轮验收范围。

## 一、功能点节点与证据

| # | 功能点 | 节点 | 证据 |
|---|---|---|---|
| 1 | 产品库 KB 五叶（0740/0467/0468/0811/0812） | ✅ 收官 | KB 卡全部回写（0230/0231 为 intermediate 目录）；要素 pageId 空属预期；菜单扫描 systemId=1 全量完成 |
| 2 | 菜单树推送伙伴平台 | ✅ 已确认（09-07） | systemId=1 全量推送完成；伙伴平台已显示内部数据（解析问题已修复） |
| 3 | 交易录制（#499 新粒度三笔） | ✅ PASS | 675 一级分类（5 步）/ 676 子分类（6 步，点「新增分类」）/ 678 产品（8 步）全 recorded + 序号 fill 步落库，stamp `20260907-1835`；证据 `tmp/product-mgmt/split-499/` |
| 4 | 基本信息保存交易 #614 | ⚠️ 差最后一环 | p2/p3 序号=1 落库（create 合约修复实证成立，05a8b66e）；**p4 QUALITY FAIL：pending_fields=法人行社 + 无保存成功 toast**；轨迹标 recorded 不可信。详见 §三-1 |
| 5 | 假成功门闩 v2 | ✅ 实证见效 | 5d6a829a：重录中首击保存报 `err-save-validation:序号` 后补填再存，未在校验红字下假绿 |
| 6 | V3 弹窗推送格式 | ✅ 收官 | traj 499 payload 树全对：popup 挂触发图标对象、18 控件 type=ele、残留弹窗剔除、popupTriggerLinked=2（90cc6f1a→03e574e6→197ea073 链）；**伙伴平台真实数据显示已验证通过（09-07 确认）** |
| 7 | 回放验证 | ⚠️ 待补 | 675/676/678 未回放（回放能力有 53 卡贯通验证背书，但产品线新产物应补一轮）；#614 待重录后一并回放 |

## 二、结论

**产品线周三跑通是大概率可达的，剩余动作全部我方可控，无外部阻塞。**

- 已闭环：KB 五叶、菜单推送、4 笔交易中 3 笔录制、V3 推送格式（伙伴侧实测）。
- 待收口：#614 重录（等字段裁决）+ 新三笔回放验证。

## 三、周三达标清单（按优先级）

### 1. #614「法人行社」字段裁决（唯一录制链缺口，今天就拍板）

- **现状**：p4 阶段 QUALITY FAIL——`pending_fields=法人行社` 且无「操作成功」toast；轨迹仍标 `recorded/isSuccessful=1`（不可信）。
- **裁决选项**：
  - **optional**：引擎把该字段从 pending 硬门放行 → 按 stamp `20260907-1926` 口径清空重录一轮即收口；
  - **硬必填**：先在 SUT 补可用法人行社选项 → 再重录。
- 重录验收口径：p2/p3 序号 fill + p4 select_option + toast「操作成功」+ stamp 落库，**不认仅 phase_done**。

### 2. 新三笔回放验证（周二插空，工作量小）

- 675/676/678 各回放一轮拿 confirmed 证据；三笔均为纯表单+树操作，无流程提交，无重复提交顾虑。
- #614 重录完成后一并回放。

### 3. 环境常规项（跑前检查）

- 控制面/执行机带最新代码重启（假成功门闩 v2、popup 清空级联删除、create 合约矫正均在近两日提交内）；重启后 `grep EADDRINUSE` 确认旧进程已死。
- 本地 DB 走 SSH 隧道（127.0.0.1:13306），跑前 `netstat :13306` 确认隧道存活（sync 分钟级延迟的教训）。
- 验收一律认 stepCount + 业务 stamp + toast 原文，不认 `recorded`/`phase_done` 自报。

## 四、遗留与移交（不卡周三）

| 项 | 处置 |
|---|---|
| SUT 残留 stamp 1835/1926 节点 | 重录收口后清理 |
| 旧 #499 单笔交易 | 未动，新粒度三笔已替代；按需作废 |
| 同名弹窗 title 回退歧义 | 待湿测证据再定改法（todo ①） |
| SPA 侧产品管理入口/徽标 | 前端仓库另行排期 |
