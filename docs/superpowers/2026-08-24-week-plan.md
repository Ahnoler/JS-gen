# 本周待办事项（2026-08-24 ~ 08-30）

> 编制日期：2026-08-24（周一）
> 主线：UI 录制（830 任务）原计划本周完成。
> 数据来源：`docs/superpowers/todo-list.md` + git `uara_V1.2`（截至 2026-08-24，本地与 origin 同步于 `d451f7a`）+ 830任务表。

---

## 一、830 任务进度（UI 录制主线）

| # | 任务 | 完成日期 | 进度 | 状态 |
|---|------|----------|------|------|
| 1 | 截图、记录元素位置（坐标） | 8.21 | **~95%（本仓侧已完成）** | 截图+坐标记录已落地并湿测通过；手动截图按钮非本项目（已移除）；步骤级高亮由前端主力，后端只按前端要求改推送结构 |
| 2 | 元素自动分级、分区 | 8.25 | **~85%** | 第一刀 tab/向导/section/titlebox/card 完成；表格/自定义网格/L1c 未覆盖 |
| 3 | 推送到自动化（截图+坐标+分区数据） | 8.25 | **~70%** | 截图+坐标已推通并湿测通过；**分区数据需改用 propertiesID/propertiesPID 父子关系表达（新 design）** |
| 4 | 录制时有测试数据（报文捞取/日志解析） | 8.30 | **~15% · 搁置** | 被测系统同事无时间协同，搁置 |

### 任务 1：截图、记录元素位置（坐标）— ~95%（本仓侧完成）

**已完成：**
- 阶段长图 + 控件坐标（PR-LOC）：CDP 滚动拼接长截图，`metadata_json.elements[]` 存全部可见 L2 控件坐标；内部滚动容器泛化修复。湿测完成。
- 步骤级 element bbox 入库（PR-LOC-HL 前置项）：`element_json` 新增 `region_id`/`region_label`/`layers[]`/`bbox`，9242 湿测通过。
- 页面级截图：`screenshot.kind='page_level'` + level_key 体系，traj 181/182 湿测通过。
- "不要浏览器头"：CDP `Page.captureScreenshot` 只截页面渲染面，天然满足。

**边界澄清（2026-08-24 用户决策）：**
- **手动截图按钮**：其他项目需求，**已移除**，不在本仓跟踪范围。
- **步骤级高亮（bbox 画框）**：前端同事主力开发；本仓后端只负责按前端要求调整推送数据结构，等前端出具体推送字段需求。

### 任务 2：元素自动分级、分区 — ~85%

**已完成：**
- 自动分区（PR-PART 第一刀）：`tab | wizard | section(collapse) | titlebox | card` 链式拼接；`display_group`/`region_label` 派生 + 碰撞去重。9242 湿测已跑，characterization 全绿。
- 自动分级 / 层次树（PR-LAYER）：每控件 `layers[]` + 整页大树 `assembleRegionTree` 已落地并接线。

**未完成：**
- 表格分区 deferred（`region_id` 短路为 `table`）；非 `el-table` 自定义网格（T5）暂缓。
- L1c LLM 分类（低置信区域兜底）P1 挂起。
- Vue 画树 / 人工分块编辑 UI：另仓前端任务。

### 任务 3：推送到自动化（截图+坐标+分级分区数据）— ~70%

**已完成：**
- V3 推送管线：截图合并进 `transcationProperties`（type 区分 page/dialog/ele），元素坐标 `rect`（JSON 字符串），页面级覆盖校验。
- 真实湿测通过：traj 33 / traj 182 推送伙伴平台返回"同步成功，共同步1条数据"，`isExport=1`。出站契约适配已修。
- **截图 + 元素位置（rect）已推通并验证。**

**未完成（本周重点）：**
- **⭐ 分级分区数据改用 `propertiesID`/`propertiesPID` 父子关系表达（2026-08-24 用户决策）**
  - 不按 `regionId`/`regionLabel` 推送——目标后端不希望增添新字段，且伙伴项目（录制工具：浏览器插件）需要格式对齐。
  - 改用现有的 `propertiesID`（自身 id）和 `propertiesPID`（父 id）父子树表达分区层级。
  - 分区目标：解决同一页面上相同名称控件（如两个保存按钮）的歧义——通过父子树让同页同名控件落在不同分区节点下可区分。
  - 当前 `propertiesPID` 只把 ele 链到 page/dialog 截图节点，分区层级（tab/section/titlebox）不是独立节点。**需新 design spec**：如何用 propertiesID/propertiesPID 编码分区层级且不加新字段、与浏览器插件伙伴格式对齐。
  - **本周开发项：design → 实施 → characterization → 湿测验证。**
- v3-payload-size ②③（构建期字段完整性校验/推送前自检/缺字段降级/超长截断）：本周可做，不改 payload 契约。
- 同名弹窗 title 回退歧义：待湿测样本再定改法。

### 任务 4：录制时有测试数据（报文捞取/日志解析/自动填写）— ~15% · 搁置

**已有底座：** case-data KV 管线（录制时表单扫描 KV 捕获）+ 录制中自动填写（`form_autofill.py`）。

**搁置（2026-08-24 用户决策）：** 报文捞取/日志解析需被测系统同事协同开发，目前被测系统同事无时间，**搁置**。待被测系统同事有时间后重启（需专刀 design）。

---

## 二、AI 录制优化项（与 UI 录制主线并行）

### 本周能做完

| 事项 | 当前状态 | 本周收尾动作 | 工时估计 |
|------|----------|--------------|----------|
| **batch-actions**（批量动作显式化） | ✅ 已实施 + 已 push（`e68f01c`）；本地验证通过 | todo-list「待 push」标注过期，更新为已交付；本地 `config/.env` 补 `MAX_ACTIONS_PER_STEP=4` | 0.5h |
| **shot-before-transition**（关键状态前置截图） | ✅ 已实施 + 已 push（`1e9e0ef`）；湿测通过 | todo-list「待 push」标注过期，更新为已交付 | 0.5h |
| **shot-wet-test**（截图补丁湿测 traj 183） | ✅ 已完成（`d451f7a` 已 push） | 状态收尾，无需动作 | 0 |
| **budget-extend**（阶段步数预算动态加成 + 耗尽续跑） | spec 待评审 | 评审 → 实施 `compute_budget_extension` + 续跑挂点 → characterization → 湿测重录 traj 33 P2 → verify-all | 2~3 天 |

### 本周做不完 / 已否决

| 事项 | 原因 |
|------|------|
| **heal-locate-wet**（回放自愈 live 湿测） | 需真实浏览器 + executor 在线环境；环境就绪可并入本周 |
| **grounding-fallback**（视觉 grounding 兜底） | ❌ 已否决（成本考量），不实施 |

---

## 三、本周聚焦排序

1. **任务③ 分区数据改 propertiesID/propertiesPID 表达**：design → 实施 → characterization → 湿测（8.25 到期项的核心缺口）。
2. **budget-extend**：评审 → 实施 → 湿测 traj 33 P2（AI 录制主线收口）。
3. **v3-payload-size ②③**：构建期字段完整性校验 + 推送前自检 + 降级策略（稳定版交付前置）。
4. **todo-list 状态收尾**：batch-actions / shot-before-transition 已 push 标注更新。
5. **heal-locate-wet**：环境可用时并入。

---

## 四、搁置 / 远期（本周不做）

| 事项 | 原因 |
|------|------|
| **任务④ 报文捞取/日志解析** | 被测系统同事无时间协同，搁置 |
| **步骤级高亮（bbox 画框）本体** | 前端主力，后端等前端推送结构要求 |
| **v3-payload-size ①**（精简传输） | 契约变更需消费方对齐，稳定版后另刀 |
| **PR-SSO-ADMIN**（管理员映射/权限闸） | 等会议结论 |
| **PR-EXEC**（脚本执行调度） | 挂起 |
| **Backlog 湿测 / 工程债** | P2/暂缓/穿插 |
